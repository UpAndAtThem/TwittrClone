import React from 'react';

const TweetModal = (props: {postId: string, handleClose: () => void}) => {
  return (
    <div className="styles.TweetModal">

    </div>
  );
}

export default TweetModal;